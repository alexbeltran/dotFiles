# This command turns "on" the VGA1
xrandr --output VGA1 --auto

# Sets the positions of montiors
xrandr --output HDMI3 --auto --right-of VGA1 --auto

# Reset the background after a resolution shift.
~/.fehbg
